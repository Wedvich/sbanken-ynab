import { TestCommandResult } from '../../../types';
import { Options, TestOptions } from '../../../../../lib/types';
export declare function test(paths: string[], options: Options & TestOptions): Promise<TestCommandResult>;
